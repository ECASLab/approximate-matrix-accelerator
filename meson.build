project('approximate-vector-accelerator', 'cpp',
  version : '1.0.0',
  default_options : ['warning_level=3'])

include_dir = include_directories('includes')
add_project_arguments('-DFAL_SW_ONLY', language : 'cpp')

subdir('test')
